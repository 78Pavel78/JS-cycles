let compLanguage = ['java', 'javaScript', 'PHP', 'C++', 'Kotlin', 'C#', 'Go', 'Python', 'Dart', 'Swift'];
let i = 0;
while (i < compLanguage.length) {
    if(compLanguage[i].length > 3) 
    console.log(compLanguage[i]);
    i++;
  }